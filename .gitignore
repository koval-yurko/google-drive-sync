.idea/
.venv/
__pycache__/
*.pyc

# secrets — never commit these
credentials.json
token.json

# git worktrees
.worktrees/

node_modules/
web/dist/
# SQLite runs in WAL mode, so the catalog is three files, not one.
*.db
*.db-shm
*.db-wal
.cache/
.pytest_cache/

# local secrets
.env
